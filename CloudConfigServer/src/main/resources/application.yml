server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Subash1010/Ideas2It-Training
          search-paths:
          - HealthCare*
        bootstrap: true
  application:
    name: application
management:
  endpoints:
    web:
      exposure:
        include: '*'